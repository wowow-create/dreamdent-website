---
import type { Lang } from '../utils/i18n';
import { t, localePath, servicesSlugs } from '../utils/i18n';

interface Props { lang: Lang; logoSrc?: string; }
const { lang, logoSrc } = Astro.props;
const tr = t(lang);
const services = servicesSlugs[lang];

const currentPath = Astro.url.pathname;

function isActive(path: string) {
  return currentPath === path || currentPath.startsWith(path + '/');
}
---

<header class="site-header" id="site-header">
  <div class="container header-inner">
    <!-- Logo -->
    <a href={localePath(lang, '/')} class="logo" aria-label="DreamDent — начало">
      {logoSrc ? (
        <img src={logoSrc} alt="DreamDent" class="logo-full" loading="eager" />
      ) : (
        <>
          <img src="/favicon.png" alt="" class="logo-icon" width="38" height="38" loading="eager" />
          <span class="logo-name">Dream<span class="logo-accent">Dent</span></span>
        </>
      )}
    </a>

    <!-- Desktop Nav -->
    <nav class="main-nav" aria-label="Главна навигация">
      <ul class="nav-list">
        <li>
          <a href={localePath(lang, '/')} class:list={['nav-link', { active: currentPath === '/' || currentPath === '/en' || currentPath === '/en/' }]}>
            {tr.nav.home}
          </a>
        </li>
        <li class="nav-dropdown">
          <button class="nav-link nav-trigger" aria-expanded="false">
            {tr.nav.services}
            <svg width="11" height="11" viewBox="0 0 12 12" fill="none" aria-hidden="true">
              <path d="M2 4l4 4 4-4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </button>
          <div class="dropdown-menu">
            <a href={services.implantology} class="dropdown-item">{tr.nav.implantology}</a>
            <a href={services.aesthetic} class="dropdown-item">{tr.nav.aesthetic}</a>
            <a href={services.periodontology} class="dropdown-item">{tr.nav.periodontology}</a>
            <a href={services.facial} class="dropdown-item">{tr.nav.facial}</a>
            <a href={services.aligners} class="dropdown-item">{tr.nav.aligners}</a>
          </div>
        </li>
        <li>
          <a href={localePath(lang, lang === 'bg' ? '/екип' : '/en/team')}
             class:list={['nav-link', { active: isActive(localePath(lang, '/екип')) }]}>
            {tr.nav.team}
          </a>
        </li>
        <li>
          <a href={localePath(lang, lang === 'bg' ? '/клинични-случаи' : '/en/clinical-cases')}
             class:list={['nav-link', { active: isActive('/клинични-случаи') || isActive('/en/clinical-cases') }]}>
            {tr.nav.cases}
          </a>
        </li>
        <li>
          <a href={localePath(lang, lang === 'bg' ? '/дентален-туризъм' : '/en/dental-tourism')}
             class:list={['nav-link', { active: isActive('/дентален-туризъм') || isActive('/en/dental-tourism') }]}>
            {tr.nav.tourism}
          </a>
        </li>
        <li>
          <a href={localePath(lang, lang === 'bg' ? '/новини' : '/en/news')}
             class:list={['nav-link', { active: isActive('/новини') || isActive('/en/news') }]}>
            {tr.nav.news}
          </a>
        </li>
        <li>
          <a href={localePath(lang, lang === 'bg' ? '/контакти' : '/en/contact')}
             class="btn btn-primary header-cta">
            {tr.nav.contact}
          </a>
        </li>
      </ul>
    </nav>

    <!-- Language Switcher + Mobile Toggle -->
    <div class="header-actions">
      <a href={lang === 'bg' ? '/en/' + Astro.url.pathname.replace(/^\//, '') : Astro.url.pathname.replace(/^\/en/, '') || '/'}
         class="lang-switch"
         aria-label={lang === 'bg' ? 'Switch to English' : 'Превключи на Български'}>
        {lang === 'bg' ? 'EN' : 'BG'}
      </a>

      <button class="mobile-toggle" id="mobile-toggle" aria-label="Отвори меню" aria-expanded="false">
        <span></span><span></span><span></span>
      </button>
    </div>
  </div>

  <!-- Mobile Menu -->
  <div class="mobile-menu" id="mobile-menu" aria-hidden="true">
    <nav>
      <a href={localePath(lang, '/')} class="mobile-link">{tr.nav.home}</a>
      <details class="mobile-details">
        <summary class="mobile-link">{tr.nav.services}</summary>
        <div class="mobile-submenu">
          <a href={services.implantology} class="mobile-sublink">{tr.nav.implantology}</a>
          <a href={services.aesthetic} class="mobile-sublink">{tr.nav.aesthetic}</a>
          <a href={services.periodontology} class="mobile-sublink">{tr.nav.periodontology}</a>
          <a href={services.facial} class="mobile-sublink">{tr.nav.facial}</a>
          <a href={services.aligners} class="mobile-sublink">{tr.nav.aligners}</a>
        </div>
      </details>
      <a href={localePath(lang, lang === 'bg' ? '/екип' : '/en/team')} class="mobile-link">{tr.nav.team}</a>
      <a href={localePath(lang, lang === 'bg' ? '/клинични-случаи' : '/en/clinical-cases')} class="mobile-link">{tr.nav.cases}</a>
      <a href={localePath(lang, lang === 'bg' ? '/дентален-туризъм' : '/en/dental-tourism')} class="mobile-link">{tr.nav.tourism}</a>
      <a href={localePath(lang, lang === 'bg' ? '/новини' : '/en/news')} class="mobile-link">{tr.nav.news}</a>
      <a href={localePath(lang, lang === 'bg' ? '/контакти' : '/en/contact')} class="mobile-link mobile-cta">{tr.nav.contact}</a>
    </nav>
  </div>
</header>

<style>
/* ── Header shell ─────────────────────────────────────────── */
.site-header {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 100;
  background: rgba(255, 255, 255, 0.96);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border-bottom: 1px solid transparent;
  transition: border-color 0.3s, box-shadow 0.3s;
}

.site-header.scrolled {
  border-color: var(--color-border);
  box-shadow: 0 1px 16px rgba(26, 18, 8, 0.07);
}

.header-inner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 68px;
  gap: 2rem;
}

/* ── Logo ─────────────────────────────────────────────────── */
.logo {
  display: flex;
  align-items: center;
  gap: 0.6rem;
  flex-shrink: 0;
  text-decoration: none;
}

.logo-icon {
  width: 38px;
  height: 38px;
  object-fit: contain;
  flex-shrink: 0;
}

.logo-full {
  max-height: 44px;
  width: auto;
  max-width: 200px;
  object-fit: contain;
  flex-shrink: 0;
}

.logo-name {
  font-family: var(--font-sans);
  font-size: 1.2rem;
  font-weight: 700;
  color: var(--color-dark);
  letter-spacing: -0.02em;
  line-height: 1;
}

.logo-accent {
  font-weight: 300;
  color: var(--color-gold);
}

/* ── Desktop Nav ──────────────────────────────────────────── */
.main-nav { display: none; }

.nav-list {
  display: flex;
  align-items: center;
  gap: 0;
}

.nav-link {
  position: relative;
  display: flex;
  align-items: center;
  gap: 0.3rem;
  padding: 0.5rem 0.75rem;
  font-size: 0.875rem;
  font-weight: 500;
  letter-spacing: 0.01em;
  color: var(--color-text-muted);
  border-radius: 0;
  transition: color 0.2s;
  background: none;
  cursor: pointer;
}

.nav-link::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 0.75rem;
  right: 0.75rem;
  height: 1.5px;
  background: var(--color-gold);
  transform: scaleX(0);
  transform-origin: center;
  transition: transform 0.25s ease;
}

.nav-link:hover { color: var(--color-dark); }
.nav-link:hover::after { transform: scaleX(1); }

.nav-link.active {
  color: var(--color-gold-dark);
}
.nav-link.active::after { transform: scaleX(1); }

.header-cta {
  font-size: 0.8rem !important;
  padding: 0.55rem 1.2rem !important;
  margin-left: 0.75rem;
}

/* ── Dropdown ─────────────────────────────────────────────── */
.nav-dropdown { position: relative; }

.dropdown-menu {
  position: absolute;
  top: calc(100% + 6px);
  left: 50%;
  transform: translateX(-50%) translateY(-6px);
  min-width: 210px;
  background: var(--color-white);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  padding: 0.375rem;
  box-shadow: 0 8px 24px rgba(26, 18, 8, 0.1);
  opacity: 0;
  pointer-events: none;
  transition: opacity 0.18s, transform 0.18s;
  z-index: 200;
}

.nav-dropdown:hover .dropdown-menu,
.nav-dropdown:focus-within .dropdown-menu {
  opacity: 1;
  pointer-events: all;
  transform: translateX(-50%) translateY(0);
}

.dropdown-item {
  display: block;
  padding: 0.6rem 0.875rem 0.6rem 1rem;
  font-size: 0.875rem;
  color: var(--color-text-muted);
  border-left: 2px solid transparent;
  border-radius: var(--radius-sm);
  transition: color 0.15s, border-color 0.15s, background 0.15s;
}

.dropdown-item:hover {
  color: var(--color-dark);
  border-left-color: var(--color-gold);
  background: var(--color-cream);
}

/* ── Language + Actions ───────────────────────────────────── */
.header-actions {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  flex-shrink: 0;
}

.lang-switch {
  font-size: 0.72rem;
  font-weight: 600;
  letter-spacing: 0.1em;
  color: var(--color-text-muted);
  padding: 0.3rem 0.6rem;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-sm);
  transition: border-color 0.2s, color 0.2s;
}

.lang-switch:hover {
  border-color: var(--color-gold);
  color: var(--color-gold);
}

/* ── Mobile Toggle ────────────────────────────────────────── */
.mobile-toggle {
  display: flex;
  flex-direction: column;
  justify-content: center;
  gap: 5px;
  width: 32px;
  height: 32px;
  padding: 4px;
}

.mobile-toggle span {
  display: block;
  height: 1.5px;
  background: var(--color-dark);
  border-radius: 2px;
  transition: transform 0.25s ease, opacity 0.2s ease;
  transform-origin: center;
}

.mobile-toggle.open span:nth-child(1) { transform: rotate(45deg) translate(4.5px, 4.5px); }
.mobile-toggle.open span:nth-child(2) { opacity: 0; transform: scaleX(0); }
.mobile-toggle.open span:nth-child(3) { transform: rotate(-45deg) translate(4.5px, -4.5px); }

/* ── Mobile Menu ──────────────────────────────────────────── */
.mobile-menu {
  display: none;
  flex-direction: column;
  background: var(--color-white);
  border-top: 1px solid var(--color-border);
  padding: 0.75rem 0 1.5rem;
  max-height: 80vh;
  overflow-y: auto;
}

.mobile-menu.open { display: flex; }

.mobile-link, .mobile-sublink {
  display: block;
  padding: 0.75rem 1.5rem;
  font-size: 0.95rem;
  color: var(--color-text);
  transition: color 0.15s, background 0.15s;
}

.mobile-link:hover { color: var(--color-gold); background: var(--color-cream); }
.mobile-cta { color: var(--color-gold) !important; font-weight: 600; }

.mobile-details summary { list-style: none; cursor: pointer; }
.mobile-details summary::-webkit-details-marker { display: none; }

.mobile-submenu { background: var(--color-cream); }
.mobile-sublink {
  padding-left: 2.5rem;
  font-size: 0.875rem;
  color: var(--color-text-muted);
  border-left: 2px solid transparent;
}
.mobile-sublink:hover { color: var(--color-gold); border-left-color: var(--color-gold); background: var(--color-cream-dark); }

/* ── Responsive ───────────────────────────────────────────── */
@media (min-width: 1024px) {
  .main-nav { display: block; }
  .mobile-toggle { display: none; }
}
</style>

<script>
  const header = document.getElementById('site-header');
  const toggle = document.getElementById('mobile-toggle');
  const menu = document.getElementById('mobile-menu');

  window.addEventListener('scroll', () => {
    header?.classList.toggle('scrolled', window.scrollY > 20);
  }, { passive: true });

  toggle?.addEventListener('click', () => {
    const open = toggle.classList.toggle('open');
    toggle.setAttribute('aria-expanded', String(open));
    menu?.classList.toggle('open', open);
    menu?.setAttribute('aria-hidden', String(!open));
  });
</script>
